Q1
SELECT CONCAT(e.first_name, ' ', e.last_name) AS Employee_Name, d.department_name AS Department_Name, FORMAT(DATEADD(MONTH, 6, e.hire_date), 'dddd, the dd of MMMM, yyyy') AS Review_Date_Description FROM dbo.employees e INNER JOIN dbo.departments d ON e.department_id = d.department_id WHERE DATEPART(WEEKDAY, DATEADD(MONTH, 6, e.hire_date)) = 6
***
